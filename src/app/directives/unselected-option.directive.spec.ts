import { UnselectedOptionDirective } from './unselected-option.directive';

describe('FilterOptionDirective', () => {
  it('should create an instance', () => {
    // const directive = new UnselectedOptionDirective();
    // expect(directive).toBeTruthy();
  });
});
