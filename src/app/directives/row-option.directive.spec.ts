import { RowOptionDirective } from './row-option.directive';

describe('RowOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new RowOptionDirective();
    expect(directive).toBeTruthy();
  });
});
